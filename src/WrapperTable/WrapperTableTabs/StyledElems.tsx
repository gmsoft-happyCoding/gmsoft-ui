import styled from 'styled-components';

export const WrapNoTab = styled.div``;

export const WrapNoTabBtnBar = styled.div`
  margin-bottom: 18px;
`;

export const WrapTab = styled.div`
  &&& > .ant-tabs > .ant-tabs-bar {
    margin: 0;
  }
`;
